server {
    listen 80;
    server_name _;

    root /var/www/cesium-drawing-demo;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 30d;
        access_log off;
    }
}
